import { AxiosPromise } from "axios"
import { httpA, ApiResponse } from "../index"
export interface HomeApiType {
    // demo(data: { sign: string }): AxiosPromise<ApiResponse>
}
export default {
    // // TODO：获取游戏次数
    // demo(data: { sign: string }) {
    //     return httpA({
    //         url: "/h5/demo",
    //         data,
    //         method: "POST"
    //     });
    // },
}
